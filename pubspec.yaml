name: my_village
description: Atmanirbhar App Making Project

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_analytics: ^5.0.2
  firebase_auth: ^0.14.0+5
  image_picker: ^0.6.7+4
  firebase_storage: ^3.1.6
  firebase_database: ^3.1.6
  fluttertoast: ^6.0.1
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - images/DedSec_logo.png
 
